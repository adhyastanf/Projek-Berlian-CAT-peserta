// SesiPage.js
import SectionHome from '@/components/sectionHome';

export default function SesiPage() {
  const userName = 'Adhyasta Naufal Faadhilah'; // Ganti dengan nama user yang dinamis jika diperlukan

  return <SectionHome userName={userName} />;
}
