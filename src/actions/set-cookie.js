'use server'

import { cookies } from "next/headers"


const setCookie = async (name, formData) => {
    const noUjian = formData.noUjian
    const kodeDesa = formData.kodeDesa
    const namaLengkap = formData.namaLengkap
    
}